/**
 * Date the generator property suites last passed in full. Stamped into every generated question's
 * `verification.passedAt`. Bump when a full local property run (PROP_SEEDS=500) passes.
 */
export const GENERATORS_VERIFIED_AT = '2026-09-24';
